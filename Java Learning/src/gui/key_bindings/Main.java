package gui.key_bindings;

public class Main {
    public static void main(String[] args) {
        /*
                Key Bindings:
                > bind an action to a keyStroke
                > doesn't require you to click a component to give it focus
                > all Swing components use Key Bindings
                > increased flexibility compared to KeyListeners
                > can assign keyStrokes to individual Swing components
                > More difficult to utilize and setup
                > Should use these over KeyListeners

         */

        new Game();
    }
}
